# config

Inspect the effective `.no-mistakes.yml` after discovery and frontend-app
resolution.

## Subcommands

| Command | Purpose |
| --- | --- |
| [`config resolve`](config-resolve.md) | Print the resolved frontend apps, Playwright coverage gates, and full-suite triggers as JSON. |
