# `no-mistakes playwright edges`

Print Playwright test-to-route and test-to-selector edges.

```sh
no-mistakes playwright edges --json
```

Use this to inspect the coverage graph behind `playwright check` and targeted
test routing.

Shared Playwright options are documented in [`playwright`](playwright.md).

Node API: `playwrightEdges(options)`.
