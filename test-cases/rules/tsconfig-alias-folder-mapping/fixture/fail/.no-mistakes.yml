rules:
  - rule: tsconfig-alias-folder-mapping
    scope: repository
    options:
      tsconfig: tsconfig.json
      baseDir: backend
      mappings:
        - prefix: "@agents"
          root: agents
