const exported = {
  projects: [
    {
      name: 'pw-destructured-bound-projects',
      testMatch: ['pw-destructured-bound-projects/**/*.spec.ts'],
    },
  ],
}

export const { projects } = exported
