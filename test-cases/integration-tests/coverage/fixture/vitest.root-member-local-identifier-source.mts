export const webConfig = {
  test: {
    projects: [
      {
        test: {
          name: 'vitest-root-member-local-identifier',
          include: ['vitest-root-member-local-identifier/**/*.test.ts'],
        },
      },
    ],
  },
}
