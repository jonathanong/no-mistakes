const config = {
  projects: [
    {
      test: {
        name: 'root-spread-default-non-null',
        include: ['root-spread-default-non-null/**/*.test.ts'],
      },
    },
  ],
}

export default config!
