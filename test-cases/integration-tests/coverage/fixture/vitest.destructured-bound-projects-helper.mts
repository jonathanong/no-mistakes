const exported = {
  projects: [
    {
      test: {
        name: 'vitest-destructured-bound-projects',
        include: ['vitest-destructured-bound-projects/**/*.test.ts'],
      },
    },
  ],
}

export const { projects } = exported
