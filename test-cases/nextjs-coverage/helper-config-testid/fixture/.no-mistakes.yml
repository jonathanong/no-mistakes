projects:
  web:
    type: nextjs
    root: web

tests:
  playwright:
    configs: playwright.config.ts
    selectors:
      testIds:
        - data-pw

rules:
  - rule: playwright-coverage
    projects:
      - web
