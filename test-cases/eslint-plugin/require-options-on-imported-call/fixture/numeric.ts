// Numeric computed keys are intentionally outside the string export-name contract.
const member = require("ssrf-guard/node")[0];
const { [0]: destructured } = require("ssrf-guard/node");

member(url);
destructured(url);
