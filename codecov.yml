coverage:
  status:
    project:
      default: off
      rust:
        # Keep in sync with RUST_COVERAGE_FAIL_UNDER_* in .github/workflows/ci.yml
        target: 99%
        threshold: 0%
        flags:
          - rust
        if_not_found: failure
      javascript:
        target: 99%
        threshold: 0%
        flags:
          - javascript
        if_not_found: failure
    patch:
      default: off
      rust:
        # Keep in sync with RUST_COVERAGE_FAIL_UNDER_* in .github/workflows/ci.yml
        target: 99%
        threshold: 0%
        flags:
          - rust
        if_not_found: failure
      javascript:
        target: 99%
        threshold: 0%
        flags:
          - javascript
        if_not_found: failure

flag_management:
  default_rules:
    carryforward: false
  individual_flags:
    - name: rust
    - name: javascript
