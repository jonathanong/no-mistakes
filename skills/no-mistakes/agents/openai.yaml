interface:
  display_name: "No Mistakes"
  short_description: "Local AST graph: impact, tests, and checks."
  default_prompt: "Use $no-mistakes to find every consumer of this symbol and the tests a change to it would need."
