projects:
  web:
    type: nextjs
    root: web

tests:
  playwright:
    configs: playwright.config.ts
    selectors:
      htmlIds: true

rules:
  - rule: playwright-coverage
    scope: repository
  - rule: playwright-unique-html-ids
    scope: repository
