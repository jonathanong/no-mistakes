projects:
  web:
    type: nextjs
    root: web

tests:
  playwright:
    configs: playwright.config.ts
    selectors:
      htmlIds: true

rules:
  - rule: playwright-unique-html-ids
    projects:
      - web
