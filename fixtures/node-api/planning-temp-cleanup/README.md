# Node planning temporary directory cleanup

`plan.json` is the small structured input used by the Node API regression tests
for generated `testsWhy` plans. The tests also use it for successful, native
rejected, and preparation-failure requests.
