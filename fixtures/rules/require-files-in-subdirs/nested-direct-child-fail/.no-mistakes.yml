rules:
  - rule: require-files-in-subdirs
    scope: repository
    options:
      packages:
        - root: packages
          requiredFiles:
            - package.json
            - enqueues/*.mts
          requireAnyOf:
            - [workers/*.mts]
