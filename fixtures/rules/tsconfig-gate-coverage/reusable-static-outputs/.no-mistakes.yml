checks:
  commands:
    - name: local
      command: [tsc, --noEmit, --project, downstream/tsconfig.json]
      fileArgs: none
      always: true
rules:
  - rule: tsconfig-gate-coverage
    scope: repository
