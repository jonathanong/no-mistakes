on: push
jobs:
  typecheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/missing
      - run: pnpm exec tsc --noEmit --project missing/tsconfig.json
