{/* The Mermaid-looking fence in this JSX attribute is text, not a diagram. */}
<DiagramCard
  description="
```mermaid
this is attribute text, not a diagram
```
"
/>

```mermaid
flowchart TD
  Real --> Diagram
```
