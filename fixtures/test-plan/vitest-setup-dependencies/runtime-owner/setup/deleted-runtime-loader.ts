// Keep the helper absent: an impact query for its deleted path must select
// this resolved setup module's owning tests through conservative fallback.
import './deleted-runtime-helper'

export const deletedRuntimeLoader = true
