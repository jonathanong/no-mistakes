tests:
  vitest:
    configs: vitest.config.ts
testPlan:
  vitest:
    environments:
      pre-push:
        groups:
          - type: dependencies
            limit:
              files: 0
          - type: dependencies
            limit:
              files: 1
rules: []
