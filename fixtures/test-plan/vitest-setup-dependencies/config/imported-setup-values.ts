// Imported literal arrays stay static; do not force a dynamic setup fallback.
export const importedSetupFiles = [
  './setup/imported-value.ts',
  './setup/imported-array.ts',
  `./setup/imported-template.ts`,
]
