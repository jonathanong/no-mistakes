<Project Sdk="Microsoft.NET.Sdk">
  <ItemGroup>
    <!-- Unknown conditions must not remove a possible compile item in static fallback. -->
    <Compile Remove="RetainedConservatively.cs" Condition="'$(Configuration)' == 'Release'" />
  </ItemGroup>
</Project>
