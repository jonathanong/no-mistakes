projects:
  repository:
    root: .
tests:
  vitest:
    configs: vitest.config.mts
testPlan:
  vitest:
    fullSuiteTriggers:
      projects:
        repository: true
    environments:
      pre-push:
        exclude:
          - src/shared.test.ts
