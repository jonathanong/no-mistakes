// Data-only registry: reachable via alias, and it does not import a localization package.
// Data-only registry: reached through the route closure without importing a
// localization package. Do not "simplify" this into a localization import.
export const labels = { home: "Home" };
