import "@react/client";
// The missing specifier is required so omitting this intermediate file from
// the derived resolve check would make allResolve true.
import { missing } from "./missing.mts";

export const value = 1;
