import "feature/specific/detail";
import "exact/value";
import "tie/value/detail";
import "replacement/value";
// The more specific mapping exists but its target does not. The catch-all
// file must remain disconnected because TypeScript does not try another key.
import "shadowed/value";
