name: CI
on:
  workflow_run:
    workflows: [missing-source]
jobs:
  test-web:
    runs-on: ubuntu-latest
    steps:
      - run: echo base
  test-backend:
    runs-on: ubuntu-latest
    steps:
      - run: echo backend
