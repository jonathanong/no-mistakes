name: CI
on: push
jobs:
  test-web:
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/actions/opencode-code-review
  test-backend:
    runs-on: ubuntu-latest
    steps:
      - run: pnpm test:backend
