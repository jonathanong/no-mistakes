name: CI
on: push
jobs:
  test-web:
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/actions/test-action
  test-backend:
    runs-on: ubuntu-latest
    steps:
      - run: pnpm test:backend
