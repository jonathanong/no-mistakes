// This origin is resolver-visible but outside the configured analysis project;
// the audit therefore proves suppression survives both re-export paths.
// no-mistakes-disable-file unique-exports: compatibility export is intentionally re-exported
export const chained = 1;
export const wildOnly = 1;
