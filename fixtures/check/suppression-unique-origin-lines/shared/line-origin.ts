// Keep the origin line distinct from its barrel's public finding line.
// This duplicate is a supported compatibility export.
export const lineOrigin = 1; // no-mistakes-disable-line unique-exports: origin compatibility export
