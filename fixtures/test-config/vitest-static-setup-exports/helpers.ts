const inner = ['./inner-setup.ts']
export const files = ['./root-setup.ts', , ...inner]
export const spread = [...inner]
const loop = loop
export const cycle = loop
const local = './named-setup.ts'
export { local as named }
