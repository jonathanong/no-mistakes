projects:
  backend:
    type: server
    queues:
      enqueues:
        - "apps/api/src/producers/**/*.mts"
      workers:
        - "apps/api/src/processors/**/*.mts"

rules:
  - rule: test-no-unmocked-dynamic-imports
    scope: repository
