# Invalid TypeScript config query fixture

The `tsconfig.json` is intentionally malformed. Do not repair it: the invalid
syntax distinguishes authoritative explicit-config errors from best-effort
automatic query fallback.
